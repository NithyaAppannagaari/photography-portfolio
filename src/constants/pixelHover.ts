/**
 * Peak “pixel block” size (CSS px) for canvas pixelation — keep PixelCard and
 * usePixelCanvas (about portrait) in sync so hover effects feel consistent.
 */
export const PORTFOLIO_PIXEL_MAX_PX = 8
